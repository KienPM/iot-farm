<?php

namespace App\Http\Controllers\Core\Contracts;

interface StoreManageContract
{
    // public function update(Store $store, Request $request, $updateField);
    // protected function createOrUpdate($storeData, $id);
    // protected function basicResult($status, $data = null);
    // protected function validateUpdateRequest($request, $store);
}
